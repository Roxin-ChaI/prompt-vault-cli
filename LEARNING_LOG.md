# Prompt Vault CLI Learning Log

## 1. Project Goal

- 我为什么选择这个项目？
- 这个项目解决了什么问题？
- 我希望通过它学习哪些 Agentic Coding 能力？

我的记录：

我选择这个项目，是因为它规模适中、功能边界清晰，适合作为我学习 AI Agent 和 Agentic Coding 的起点。通过完成这个项目，我不仅可以学习如何编写结构清晰、约束明确的 Prompt，还可以理解怎样让 Agent 按照需求和实现计划逐步完成开发任务。

这个工具解决了 Prompt 分散、难以查找和重复管理的问题。它可以将常用 Prompt 保存在本地 JSON 文件中，并通过命令行完成添加、列出、搜索和删除操作。

通过这个项目，我希望学习如何定义需求、拆分任务、向 Agent 提供明确的文件范围和验收条件、检查 Agent 是否越界修改、运行自动化测试、进行代码审查，以及使用 Git 对每个开发阶段进行版本管理。

## 2. What I Built

- 最终项目包含哪些功能？
- 数据模型、存储层和 CLI 分别负责什么？
- 数据从终端命令到 `data.json` 经历了什么流程？

我的记录：

我完成了一个用于管理 Prompt 的本地命令行工具。它支持添加、列出、搜索和删除 Prompt，并将数据持久化保存在本地的 `data.json` 文件中。项目还包含模型、存储层和 CLI 的自动化测试，用于验证正常操作和各种错误情况。

数据模型 `Prompt` 定义了一条 Prompt 的结构，包括名称、内容和可选描述。它还负责验证字段是否为空、清理首尾空格，并保证创建后的 Prompt 对象不可修改。

存储层 `PromptStorage` 负责读取和写入 JSON 文件。它将 JSON 数据转换为 `Prompt` 对象，管理 Prompt 集合，检查重复名称，完成搜索和删除，并通过临时文件和 `os.replace()` 实现更安全的原子写入。

CLI 层负责解析用户在终端输入的命令和参数，调用数据模型及存储层完成实际操作，然后将成功信息输出到 `stdout`，将错误信息输出到 `stderr`，并返回相应的退出码。

当用户执行 `add` 命令时，程序首先解析终端参数，然后使用这些参数创建并验证 `Prompt` 对象。存储层读取现有的 `data.json`，检查是否存在重复名称，将新 Prompt 加入集合，最后先写入临时文件，再原子替换正式的 `data.json` 文件。完成后，CLI 向用户显示添加成功的信息。

## 3. Agentic Coding Workflow

本项目采用了以下流程：

1. 定义需求
2. 制定并审批实现计划
3. 分阶段让 Agent 实现
4. 审查 Agent 的实际修改范围
5. 运行自动化测试
6. 执行真实 CLI 集成测试
7. 进行只读代码审查
8. 根据审查结果修正
9. 使用 Git 分阶段提交
10. 完成项目文档

我对这套流程的理解：

首先需要明确项目要解决的问题、功能范围、错误行为和验收标准，然后让 Agent 阅读需求并提出实现计划。计划不能直接执行，我需要先检查它是否遗漏功能、设计是否合理、任务拆分是否清晰。

计划通过后，再让 Agent 分阶段实现，每个阶段只完成一个明确目标，并限制允许修改的文件范围。Agent 完成后，我需要检查 Git 状态和代码差异，确认它没有越界修改，也没有实现计划之外的功能。

接下来运行自动化测试，并通过真实的 CLI 命令检查程序在实际环境中的行为。测试不仅要覆盖成功路径，也要覆盖重复名称、错误参数、损坏文件和不存在的数据等失败路径。

测试通过后，还要进行代码审查，对照需求和实现计划检查代码质量、异常处理和设计一致性。发现问题后，应单独修正并重新测试。只有代码、测试、手动操作、文档和需求都保持一致时，才能完成验收。

整个过程中使用 Git 分阶段提交，使每次功能实现、问题修复和文档修改都有清晰记录，也方便发现 Agent 的错误并回退修改。

## 4. Important Design Decisions

请记录本项目中的关键设计选择，例如：

- 为什么使用不可变的 `Prompt` dataclass？
- 为什么使用 `casefold()` 比较名称？
- 为什么保存时使用临时文件和 `os.replace()`？
- 为什么通过 `PromptStorage` 注入临时路径进行测试？
- 为什么区分退出码 `0`、`1` 和 `2`？


这个项目中有几个重要的设计选择。

首先，`Prompt` 使用不可变 dataclass 作为数据模型。这样可以保证 Prompt 创建后不会被意外修改，使数据状态更加稳定。同时，所有字段验证和格式规范化都集中在模型层，避免不同模块重复处理数据规则。

其次，Prompt 名称比较使用 `casefold()` 而不是简单的大小写转换。因为 Prompt 名称在实际使用中应该认为大小写不同的字符串是同一个名称，例如 `Summarize Text` 和 `summarize text` 不应该同时存在。与此同时，程序仍然保留用户输入的原始大小写，提高可读性。

第三，存储层没有直接覆盖原始 JSON 文件，而是先写入同目录下的临时文件，然后使用 `os.replace()` 替换正式文件。这样可以降低写入过程中程序异常导致数据损坏的风险。

第四，测试过程中使用依赖注入方式传入临时 `PromptStorage`。这样测试不会依赖真实的 `data.json`，每个测试拥有独立环境，提高了测试可靠性，也方便模拟不同错误情况。

最后，CLI 使用不同退出码表示不同结果：
- `0` 表示成功执行；
- `1` 表示应用逻辑或存储错误；
- `2` 表示命令参数错误。

这种设计让程序不仅可以被用户直接使用，也方便脚本自动判断执行结果。

## 5. How I Directed the Agent

- 我是如何限制 Agent 修改文件范围的？
- 我如何给出验收条件和测试要求？
- 我为什么要求 Agent 不自动提交代码？
- 我如何使用 Git 检查 Agent 是否越界修改？

我的记录：

在这个项目中，我没有直接让 Agent 一次性生成完整代码，而是通过明确的任务拆分和约束逐步引导 Agent 完成开发。

首先，我让 Agent 阅读需求文档，并要求它先制定实现计划。在执行计划之前，我检查了功能范围、模块划分、测试策略以及实现顺序，确认计划符合项目目标后才开始编码。

在每个开发阶段，我都会限定 Agent 可以修改的文件范围，并要求它报告：
- 修改了哪些文件；
- 完成了哪些功能；
- 运行了哪些测试；
- 是否存在假设。

这种方式可以避免 Agent 在不了解整体目标的情况下进行大范围修改。

同时，我要求 Agent 不自动提交 Git，而由我控制提交过程。每个阶段完成后，我通过 `git status`、`git diff` 和测试结果确认修改内容，再决定是否提交。

在测试阶段，我不仅关注测试是否通过，还检查测试是否覆盖错误情况。例如重复 Prompt、损坏 JSON 文件、错误参数和不存在的数据等场景。

通过这个过程，我认识到使用 AI Agent 的关键不是让它替代开发者，而是通过清晰的需求、边界条件和验收标准，让 Agent 成为一个可控的开发协作者。

## 6. What the Agent Did Well

- 哪些任务适合交给 Agent？
- Agent 在实现、测试、文档或代码审查中表现好的地方是什么？
- 哪些明确约束帮助 Agent 得到了更可靠的结果？

我的记录：

在这个项目中，Agent 在代码实现、测试编写和文档生成方面提高了开发效率。

首先，Agent 很适合处理明确规则下的代码实现任务。例如根据实现计划创建数据模型、存储层和 CLI 模块，并根据已有测试要求补充对应测试代码。当需求、文件范围和验收条件明确时，Agent 能够快速完成大量重复性工作。

其次，Agent 在测试设计方面提供了较好的帮助。除了实现主要功能测试外，它还能够根据需求补充错误场景测试，例如重复 Prompt、无效 JSON、读取异常、空文件和命令行错误参数等情况。

另外，通过让 Agent 输出修改文件列表、测试结果和假设，可以更容易检查它的行为是否符合预期。明确的约束减少了 Agent 自主扩展范围导致的问题。

但是，Agent 并不能完全替代开发者。项目中的需求判断、架构选择、验收标准制定以及最终代码审查仍然需要开发者参与。高质量的 Agentic Coding 依赖于开发者提供清晰的问题定义和持续反馈。

## 7. Problems and Corrections

记录本项目中出现过的问题，例如：

- Markdown 代码块嵌套导致指令格式错误
- `.DS_Store` 意外出现在 Git 状态中
- 实现计划中的函数签名与实际实现发生漂移
- `main()` 中存在不合理的默认成功返回
- 初始测试缺少真实模块入口、无效 UTF-8 和空文件覆盖

对于每个问题，记录：

- 问题是什么？
- 我是如何发现的？
- 最终如何修正？
- 下次怎样更早避免？

我的记录：

在项目开发过程中，遇到了一些问题，并通过检查和反馈逐步修正。

第一个问题是早期编写 Agent 指令时，Markdown 代码块格式出现错误，导致任务描述可能无法被 Agent 正确理解。通过检查 Prompt 结构，重新整理代码块格式后，解决了这个问题。这让我认识到，给 Agent 的指令本身也需要像代码一样保持清晰和规范。

第二个问题是 Git 状态中出现了 `.DS_Store` 文件。这个文件属于 macOS 系统生成的元数据，不应该进入项目仓库。通过修改 `.gitignore` 增加 `.DS_Store` 规则，并提交修正，避免类似文件污染仓库。

第三个问题是在代码审查阶段发现实现计划和实际代码存在轻微漂移。例如最初计划中的 `main()` 接口与实际采用的依赖注入方式不同。通过更新实现计划，使文档和代码保持一致。

第四个问题是初始测试覆盖范围不足。代码虽然通过了主要功能测试，但审查后发现缺少一些异常情况测试，例如无效 UTF-8 文件、读取异常、空数据文件以及真实模块入口测试。根据审查建议补充测试后，提高了项目可靠性。

这些问题让我认识到，使用 AI Agent 开发时，测试通过并不代表项目一定完整。需求、设计、实现、测试和文档需要保持一致，持续审查和反馈是保证质量的重要步骤。

## 8. Testing and Verification

本项目使用了：

- 模型单元测试
- 存储层单元测试
- CLI 单元测试
- 临时文件与依赖注入
- `capsys`
- `monkeypatch`
- 子进程冒烟测试
- 手动 CLI 集成测试
- `compileall`
- `git diff --check`

最终自动化测试结果：

- `116 passed`

我的记录：

本项目采用多层次验证方式，而不是只依赖单次测试结果。

首先，针对不同模块分别编写测试：
- `test_models.py` 验证 Prompt 数据模型的字段验证、规范化和不可变特性；
- `test_storage.py` 验证 JSON 文件读写、重复检测、搜索、删除以及各种存储异常；
- `test_cli.py` 验证命令行参数解析、输出格式、退出码和真实模块入口行为。

测试过程中不仅覆盖正常使用流程，也覆盖异常情况。例如：
- 添加重复名称的 Prompt；
- 搜索不存在的 Prompt；
- 删除不存在的 Prompt；
- 空 JSON 文件；
- 损坏 JSON 文件；
- 无效 UTF-8 文件；
- 文件读取异常；
- 错误命令参数。

除此之外，我还进行了手动 CLI 集成测试，通过真实命令确认：
- Prompt 可以成功添加；
- 数据能够保存到 `data.json`；
- list、search、delete 命令符合预期；
- 错误信息能够正确输出。

最终验证结果：

- `python -m pytest -v`
  - 结果：116 passed

- `python -m compileall src tests`
  - 结果：成功

- `git diff --check`
  - 结果：无格式错误

通过这些验证，我认识到测试不仅用于证明代码可以运行，也用于验证设计是否符合需求，以及帮助发现 Agent 实现中的遗漏。

## 9. Git Workflow

记录本项目中使用的 Git 工作方式：

- 每个功能阶段单独提交
- 提交前检查 `git status`
- 只暂存目标文件
- 使用 `git diff --cached --check`
- 文档、功能、修复和环境清理使用不同提交
- `data.json`、虚拟环境和缓存文件不进入仓库

我学到的 Git 实践：

我学到 Git 不只是用于保存和上传代码，也可以用于控制和审查 Agent 的修改。

在执行 `git add` 之前，需要反复运行测试和检查命令，例如：

- 使用 `git status` 检查哪些文件发生了变化；
- 使用 `git diff` 审查具体修改内容；
- 运行 `pytest` 验证自动化测试；
- 使用 `compileall` 检查 Python 文件能否正常编译；
- 通过真实 CLI 命令验证程序的实际行为；
- 使用 `git diff --check` 检查基础格式问题。

只有确认功能正确、测试通过、修改范围符合任务要求后，才将目标文件加入暂存区。

执行 `git add` 后，还需要通过 `git status`、`git diff --cached` 和 `git diff --cached --check` 再次检查暂存内容，确保没有误加入无关文件，然后才能执行 `git commit`。

我还学到了应当按照功能阶段进行小步提交。需求文档、功能实现、错误修复、环境配置和 README 应尽量使用不同的提交记录。这样每次提交的目的更加明确，也便于审查、定位问题和回退代码。

在这个项目中，我没有让 Agent 自动提交代码，而是由我亲自完成检查、暂存和提交。这保证了最终进入 Git 历史的内容经过了人工确认。

## 10. Key Lessons

请至少写出 5 条最重要的收获。

1. 在让 Agent 编码之前，我需要先明确：

需求是什么、目标是什么、验收标准是什么，以及实现过程中有哪些限制和边界。好的 Agent 任务不是简单描述功能，而是提供清晰的问题定义和约束条件。

2. 审核 Agent 实现计划时，我需要重点检查：

Agent 是否正确理解需求，任务拆分是否合理，模块设计是否符合预期，以及测试方案是否能够验证功能。

3. Agent 完成代码后，我不能只看测试结果，还需要：

检查代码实现是否完整，是否覆盖全部需求，修改范围是否符合要求，以及代码设计是否与最初计划保持一致。

4. Git 在 Agentic Coding 工作流中的作用是：

管理代码版本，记录每个开发阶段的变化，同时作为检查 Agent 修改范围的重要工具。通过 `git status`、`git diff` 和提交记录，可以确认代码是否符合预期。

5. 通过这个项目，我对“开发者与 Agent 的关系”的理解是：

开发者负责定义问题、制定目标、审核方案和最终验收；Agent 负责根据明确的需求和约束执行代码实现、测试补充和文档整理。Agent 不是替代开发者，而是在开发者管理下提高开发效率的协作者。

## 11. What I Can Now Do Independently

完成这个项目后，我已经能够独立完成：

- 将一个模糊的需求转换为明确的功能描述、约束条件和验收标准；
- 编写结构清晰的 Prompt，让 Agent 理解任务目标和修改范围；
- 要求 Agent 先制定实现计划，并在执行前审核计划的合理性；
- 将较大的开发任务拆分成多个小阶段，让 Agent 逐步实现；
- 通过 `git status`、`git diff` 和代码审查检查 Agent 的实际修改；
- 使用自动化测试和手动 CLI 测试验证功能是否符合需求；
- 根据测试结果和代码审查反馈，让 Agent 进行针对性修正；
- 使用 Git 对功能开发、问题修复和文档更新进行版本管理。

## 12. Next Improvements

下一次项目中，我准备改进：

- Prompt 编写方式：未来需要进一步提高任务描述的精确度，包括明确输入、输出、限制条件、验收标准和禁止事项，减少 Agent 对需求的误解。
- Agent 任务拆分方式：对于更复杂的项目，需要提前设计更细粒度的开发阶段，避免一次给予 Agent 过大的任务范围。
- 测试策略：未来会在实现前设计测试方案，让测试标准成为需求的一部分，而不是开发完成后的补充。
- 代码审查方式：除了检查功能是否正确，还需要更加关注架构设计、代码可维护性和未来扩展能力。
- Git 工作流：继续保持小步提交的方式，每个提交只包含一个明确目标，使项目历史更加清晰，也方便定位问题。
- 开发效率：未来希望建立更加标准化的 Agent 工作模板，包括需求分析 Prompt、代码实现 Prompt、测试 Prompt 和代码审查 Prompt。

## 13. Next Project

我的下一个 Agentic Coding 项目计划：

- 项目名称：Prompt Vault API
- 要解决的问题：将当前只能在本地终端运行的 Prompt 管理工具升级为可以通过 HTTP 接口访问的工程化服务，并支持数据库持久化、修改操作和统一错误响应。
- 计划使用的技术：Python 3.12、FastAPI、Pydantic、SQLAlchemy、SQLite、Alembic、pytest、Ruff、Docker 和 GitHub Actions。
- 希望重点训练的 Agentic Coding 能力：在分层架构中拆分 Agent 任务，定义 API 契约，限制 Agent 的修改范围，通过单元测试和集成测试验证实现，并使用 Issue、功能分支、Pull Request、CI 和 Release 完成完整的软件交付流程。

这个项目暂时不加入 LLM、RAG、MCP、LangGraph 或多 Agent。完成软件工程阶段后，再进入 AI Agent Fundamentals，学习 Tool Calling、Memory、RAG 和工作流编排。